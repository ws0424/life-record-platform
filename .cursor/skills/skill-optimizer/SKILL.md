---
name: skill-optimizer
description: Skill 优化工具。当用户说"优化skill"、"修复skill"、"optimize skill"或"fix skill"时使用此技能。根据 skill-validator 的检查报告自动修复和优化 skills，包括清理空目录、修复 YAML frontmatter、完善 description、删除不规范文件、优化文件组织等。通常在执行"检查skill"后使用，自动应用修复建议。
---

# Skill 优化工具

根据 skill-validator 的检查报告自动修复和优化 skills，确保符合 OpenSkills 标准。

## 快速开始

当用户请求优化 skill 时：

1. **读取验证报告** - 获取 skill-validator 的检查结果
2. **分析问题** - 识别需要修复的问题
3. **应用修复** - 自动执行优化操作
4. **验证结果** - 再次检查确保问题已解决
5. **生成报告** - 列出所有修复操作

## 优化功能

### 1. 目录结构优化

**自动修复：**
- ✅ 清理空的 `references/`、`scripts/`、`assets/` 目录
- ✅ 删除不规范的文件（README.md、CHANGELOG.md 等）
- ✅ 创建缺失的 SKILL.md 文件
- ✅ 重命名不规范的目录（docs/ → references/）

**示例：**
```bash
# 清理空目录
rm -rf .claude/skills/skill-name/references/
rm -rf .claude/skills/skill-name/scripts/

# 删除不规范文件
rm .claude/skills/skill-name/README.md
rm .claude/skills/skill-name/CHANGELOG.md
```

### 2. YAML Frontmatter 优化

**自动修复：**
- ✅ 添加缺失的 `name` 字段
- ✅ 添加缺失的 `description` 字段
- ✅ 删除多余的字段（version、author、tags 等）
- ✅ 确保 name 与文件夹名称一致
- ✅ 格式化 frontmatter

**示例：**

**优化前：**
```yaml
---
name: skill-name
version: 1.0.0
author: Someone
tags: [tag1, tag2]
description: 简短描述
---
```

**优化后：**
```yaml
---
name: skill-name
description: 详细描述。当用户说"xxx"时使用此技能。提供xxx功能，包括xxx、xxx等。适用于xxx场景。
---
```

### 3. Description 优化

**自动增强：**
- ✅ 扩展过短的 description（<100 字符）
- ✅ 添加触发条件（"当用户说..."）
- ✅ 添加功能说明
- ✅ 添加使用场景
- ✅ 添加适用范围

**优化模板：**
```yaml
description: [工具名称]。当用户说"[触发词1]"、"[触发词2]"或"[触发词3]"时使用此技能。[功能说明]，包括[功能1]、[功能2]、[功能3]等。适用于[使用场景]。
```

**示例：**

**优化前：**
```yaml
description: 代码优化工具
```

**优化后：**
```yaml
description: 代码检查与优化工具。当用户说"检查代码"、"优化代码"、"code review"、"optimize code"或"clean code"时使用此技能。自动清理无效代码（console语句、未使用的导入/变量/方法）、优先使用lodash方法库优化代码、改进错误处理、统一代码规范（ES6+、模板字符串、可选链等）、优化Vue组件性能。适用于JavaScript/Vue.js项目的代码质量提升。
```

### 4. 文件组织优化

**自动优化：**
- ✅ 将详细内容从 SKILL.md 移到 references/
- ✅ 确保 references/ 中的文件被引用
- ✅ 优化文件命名（使用大写，如 PATTERNS.md）
- ✅ 添加文件引用链接

**示例：**

如果 SKILL.md 过长（>500 行），自动拆分：

```markdown
# SKILL.md (保留核心内容)
## 快速开始
...

## 详细模式
查看 [PATTERNS.md](references/PATTERNS.md) 获取完整模式列表。

---

# references/PATTERNS.md (详细内容)
# 完整模式列表
...
```

### 5. 内容质量优化

**自动改进：**
- ✅ 添加缺失的章节（快速开始、示例、最佳实践）
- ✅ 格式化代码块
- ✅ 统一标题层级
- ✅ 添加目录（如果内容较长）
- ✅ 优化示例代码

## 工作流程

### 步骤 1：读取验证报告

从 skill-validator 的输出中提取问题：

```javascript
const issues = {
  highPriority: [
    { type: 'missing_file', file: 'SKILL.md' },
    { type: 'missing_field', field: 'description' }
  ],
  mediumPriority: [
    { type: 'short_description', length: 20 },
    { type: 'extra_file', file: 'README.md' }
  ],
  lowPriority: [
    { type: 'empty_directory', dir: 'references/' },
    { type: 'empty_directory', dir: 'scripts/' }
  ]
}
```

### 步骤 2：生成修复计划

根据问题优先级生成修复计划：

```markdown
## 🔧 修复计划

### 高优先级（立即修复）
1. 创建 SKILL.md 文件
2. 添加 description 字段

### 中优先级（建议修复）
3. 扩展 description（当前 20 字符 → 建议 >100 字符）
4. 删除 README.md 文件

### 低优先级（可选修复）
5. 清理空的 references/ 目录
6. 清理空的 scripts/ 目录
```

### 步骤 3：执行修复

按优先级依次执行修复操作：

```javascript
// 1. 高优先级修复
if (missing_skill_md) {
  createSkillMd(skillName)
}

if (missing_description) {
  addDescription(skillName)
}

// 2. 中优先级修复
if (short_description) {
  enhanceDescription(skillName)
}

if (extra_files) {
  removeExtraFiles(skillName)
}

// 3. 低优先级修复
if (empty_directories) {
  cleanEmptyDirectories(skillName)
}
```

### 步骤 4：验证修复

修复后再次运行 skill-validator 验证：

```markdown
## ✅ 修复验证

### 修复前
- ❌ 缺少 SKILL.md
- ❌ Description 过短（20 字符）
- ⚠️ 存在 README.md

### 修复后
- ✅ SKILL.md 已创建
- ✅ Description 已完善（156 字符）
- ✅ README.md 已删除

### 验证结果
所有问题已修复！✅
```

### 步骤 5：生成报告

生成详细的修复报告：

```markdown
## 📊 优化报告

### 优化的 Skill: example-skill

### 执行的操作
1. ✅ 删除 README.md
2. ✅ 删除 frontmatter 中的 version 字段
3. ✅ 删除 frontmatter 中的 author 字段
4. ✅ 扩展 description（20 → 156 字符）
5. ✅ 清理空的 references/ 目录
6. ✅ 清理空的 scripts/ 目录

### 优化前后对比

#### Frontmatter
**优化前:**
```yaml
---
name: example-skill
version: 1.0.0
author: Someone
description: 工具
---
```

**优化后:**
```yaml
---
name: example-skill
description: 示例工具。当用户说"使用示例"、"example"时使用此技能。提供示例功能，包括演示、测试等。适用于学习和测试场景。
---
```

### 优化结果
- 修复问题数: 6
- 高优先级: 2
- 中优先级: 2
- 低优先级: 2
- 耗时: 2 秒

### 验证状态
✅ 所有问题已修复，skill 现在符合 OpenSkills 标准！
```

## 优化策略

### 策略 1：保守优化（默认）

只修复明确的问题，不改变原有内容：

- ✅ 删除多余字段
- ✅ 清理空目录
- ✅ 删除不规范文件
- ❌ 不修改现有 description
- ❌ 不重构内容结构

**适用场景：** 已有完善内容的 skills

### 策略 2：积极优化

在保守优化基础上，增强内容质量：

- ✅ 扩展过短的 description
- ✅ 添加缺失的章节
- ✅ 优化文件组织
- ✅ 改进示例代码
- ⚠️ 保留原有核心内容

**适用场景：** 内容不完善的 skills

### 策略 3：完全重构

彻底优化 skill 结构和内容：

- ✅ 重写 description
- ✅ 重组内容结构
- ✅ 拆分长文件
- ✅ 添加完整示例
- ⚠️ 需要用户确认

**适用场景：** 严重不符合规范的 skills

## 使用示例

### 示例 1：修复 skill-validator 发现的问题

**场景：** skill-validator 发现 skill-validator 有空目录

**输入：**
```
优化skill
```

**执行操作：**
```bash
# 清理空目录
rm -rf .claude/skills/skill-validator/references/
rm -rf .claude/skills/skill-validator/scripts/
```

**输出：**
```markdown
## 📊 优化报告

### 优化的 Skill: skill-validator

### 执行的操作
1. ✅ 清理空的 references/ 目录
2. ✅ 清理空的 scripts/ 目录

### 优化结果
- 修复问题数: 2
- 低优先级: 2
- 耗时: 0.5 秒

### 验证状态
✅ 所有问题已修复！
```

### 示例 2：优化不规范的 Skill

**场景：** 发现一个严重不规范的 skill

**问题：**
- 有 README.md 文件
- frontmatter 有多余字段
- description 过短

**输入：**
```
优化skill bad-skill
```

**执行操作：**
1. 删除 README.md
2. 清理 frontmatter
3. 扩展 description

**输出：**
```markdown
## 📊 优化报告

### 优化的 Skill: bad-skill

### 执行的操作
1. ✅ 删除 README.md
2. ✅ 删除 frontmatter 中的 version 字段
3. ✅ 删除 frontmatter 中的 author 字段
4. ✅ 删除 frontmatter 中的 tags 字段
5. ✅ 扩展 description（15 → 142 字符）

### 优化前后对比

#### 文件结构
**优化前:**
```
bad-skill/
├── README.md          ❌
├── SKILL.md
└── scripts/
```

**优化后:**
```
bad-skill/
└── SKILL.md           ✅
```

#### Frontmatter
**优化前:**
```yaml
---
name: bad-skill
version: 1.0.0
author: Someone
tags: [demo]
description: 工具
---
```

**优化后:**
```yaml
---
name: bad-skill
description: 演示工具。当用户说"演示"、"demo"时使用此技能。提供演示功能，包括示例展示、功能测试等。适用于学习和演示场景。
---
```

### 优化结果
- 修复问题数: 5
- 高优先级: 0
- 中优先级: 4
- 低优先级: 1
- 耗时: 1.5 秒

### 验证状态
✅ 所有问题已修复，skill 现在符合 OpenSkills 标准！
```

### 示例 3：批量优化所有 Skills

**输入：**
```
优化所有skill
```

**输出：**
```markdown
## 📊 批量优化报告

### 优化统计
- 总 Skills 数: 3
- 需要优化: 2
- 无需优化: 1

---

### ✅ code-optimization
**状态:** 无需优化
- 已完全符合标准

---

### ✅ skill-validator
**状态:** 已优化

**执行的操作:**
1. ✅ 清理空的 references/ 目录
2. ✅ 清理空的 scripts/ 目录

**修复问题数:** 2

---

### ✅ skill-optimizer
**状态:** 已优化

**执行的操作:**
1. ✅ 清理空的 references/ 目录
2. ✅ 清理空的 scripts/ 目录

**修复问题数:** 2

---

### 总体结果
- 总修复问题数: 4
- 高优先级: 0
- 中优先级: 0
- 低优先级: 4
- 总耗时: 1.2 秒

✅ 所有 skills 现在都符合 OpenSkills 标准！
```

## 自动化修复规则

### 规则 1：空目录清理

```javascript
if (directory.isEmpty() && directory.isOptional()) {
  removeDirectory(directory)
}
```

### 规则 2：不规范文件删除

```javascript
const invalidFiles = ['README.md', 'CHANGELOG.md', 'INSTALLATION_GUIDE.md']
if (invalidFiles.includes(filename)) {
  removeFile(filename)
}
```

### 规则 3：Frontmatter 清理

```javascript
const allowedFields = ['name', 'description']
const extraFields = Object.keys(frontmatter).filter(
  key => !allowedFields.includes(key)
)
extraFields.forEach(field => delete frontmatter[field])
```

### 规则 4：Description 增强

```javascript
if (description.length < 100) {
  description = enhanceDescription({
    name: skillName,
    currentDescription: description,
    minLength: 100
  })
}
```

### 规则 5：文件拆分

```javascript
if (skillMd.lines > 500) {
  const { main, references } = splitContent(skillMd)
  writeFile('SKILL.md', main)
  writeFile('references/DETAILS.md', references)
}
```

## 最佳实践

### 应该做 ✅
- 优化前先运行 skill-validator
- 备份原始文件（自动创建 .backup）
- 优化后再次验证
- 查看优化报告确认更改
- 测试 skill 功能是否正常

### 不应该做 ❌
- 不要在没有验证的情况下优化
- 不要删除有内容的目录
- 不要修改核心功能逻辑
- 不要改变 skill 的原始意图
- 不要跳过验证步骤

## 安全机制

### 1. 备份机制

优化前自动备份：

```bash
# 创建备份
cp -r .claude/skills/skill-name .claude/skills/skill-name.backup

# 如果优化失败，可以恢复
mv .claude/skills/skill-name.backup .claude/skills/skill-name
```

### 2. 确认机制

对于重大更改，需要用户确认：

```markdown
## ⚠️ 需要确认

以下操作将对 skill 进行重大更改：

1. 删除 README.md（包含 150 行内容）
2. 重写 description（当前 15 字符 → 建议 150 字符）
3. 拆分 SKILL.md（当前 650 行 → 拆分为 2 个文件）

是否继续？[Y/n]
```

### 3. 回滚机制

如果优化后验证失败，自动回滚：

```javascript
try {
  optimize(skill)
  const result = validate(skill)
  if (!result.passed) {
    rollback(skill)
    throw new Error('优化后验证失败，已回滚')
  }
} catch (error) {
  rollback(skill)
  console.error('优化失败:', error)
}
```

## 配置选项

### 优化级别

```yaml
optimization_level: conservative  # conservative | moderate | aggressive

conservative:
  - 只修复明确的错误
  - 不修改现有内容
  
moderate:
  - 修复错误
  - 增强不完善的内容
  
aggressive:
  - 完全重构
  - 需要用户确认
```

### 自动化程度

```yaml
automation: semi-auto  # manual | semi-auto | full-auto

manual:
  - 生成修复建议
  - 需要用户手动执行
  
semi-auto:
  - 自动执行低风险操作
  - 高风险操作需要确认
  
full-auto:
  - 自动执行所有操作
  - 创建备份
```

## 注意事项

1. **始终先验证** - 使用 skill-validator 检查后再优化
2. **查看报告** - 仔细查看优化报告，确认更改合理
3. **测试功能** - 优化后测试 skill 是否正常工作
4. **保留备份** - 重要的 skills 手动备份
5. **渐进优化** - 先用保守模式，确认无误后再用积极模式

## 输出格式

```markdown
## 📊 Skill 优化报告

**优化时间**: 2024-01-26 15:00:00
**优化策略**: 保守优化

### 优化的 Skill: [skill-name]

### 📋 执行的操作
1. ✅ [操作1]
2. ✅ [操作2]
3. ✅ [操作3]

### 📊 优化统计
- 修复问题数: X
- 高优先级: X
- 中优先级: X
- 低优先级: X
- 耗时: X 秒

### 🔍 优化前后对比
[详细对比]

### ✅ 验证结果
[验证状态]

### 💾 备份位置
`.claude/skills/[skill-name].backup`
```

